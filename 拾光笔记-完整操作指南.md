# 拾光笔记：完整操作指南

> 最后更新：2026-09-19  
> 技术方案：Hexo + Fluid + GitHub Pages

## 目录

1. [项目位置与常用入口](#一项目位置与常用入口)
2. [修改博客名称和基本信息](#二修改博客名称和基本信息)
3. [修改头像](#三修改头像)
4. [修改个人简介和社交链接](#四修改个人简介和社交链接)
5. [修改关于页面](#五修改关于页面)
6. [修改首页标语、横幅和页脚](#六修改首页标语横幅和页脚)
7. [新建和编辑文章](#七新建和编辑文章)
8. [在文章中插入图片](#八在文章中插入图片)
9. [本地预览和生成网站](#九本地预览和生成网站)
10. [发布到 GitHub Pages](#十发布到-github-pages)
11. [以后如何更新网站](#十一以后如何更新网站)
12. [常见问题](#十二常见问题)
13. [文件结构速查](#十三文件结构速查)

---

## 一、项目位置与常用入口

博客项目位于：

```text
C:\Users\xwh\Documents\Codex\2026-09-19\ji\outputs\hexo-blog
```

项目中有三个便捷入口：

| 文件 | 用途 |
|---|---|
| `启动博客.cmd` | 启动本地预览 |
| `新建文章.cmd` | 输入标题并创建文章 |
| `生成网站.cmd` | 生成静态网站到 `public` 文件夹 |

推荐使用 Visual Studio Code 打开整个博客文件夹：

1. 打开 VS Code；
2. 选择“文件 → 打开文件夹”；
3. 选择上面的 `hexo-blog` 文件夹。

也可以在 PowerShell 中执行：

```powershell
cd "C:\Users\xwh\Documents\Codex\2026-09-19\ji\outputs\hexo-blog"
code .
```

---

## 二、修改博客名称和基本信息

打开项目根目录中的 `_config.yml`。

需要修改的主要字段：

```yaml
title: 拾光笔记
subtitle: 记录技术、设计与生活的微光
description: 一个关于技术实践、设计思考与日常生活的个人博客
keywords: Hexo, Fluid, 静态博客, 技术, 设计, 生活
author: 你的名字
language: zh-CN
timezone: Asia/Shanghai
```

示例：

```yaml
title: 时光笔记
subtitle: 记录学习、工作与生活
description: 我的个人学习和生活记录
keywords: 编程, 学习, 周记, 生活
author: 张三
language: zh-CN
timezone: Asia/Shanghai
```

说明：

- `title`：浏览器标题和站点名称；
- `subtitle`：博客副标题；
- `description`：搜索引擎和页面描述；
- `keywords`：博客关键词，用英文逗号分隔；
- `author`：文章作者名称。

如果修改了 `title`，还要同步修改 `_config.fluid.yml` 中的：

```yaml
navbar:
  blog_title: "时光笔记"
```

---

## 三、修改头像

### 1. 准备头像

建议使用正方形图片，例如：

- `avatar.png`
- `avatar.jpg`
- 建议尺寸：500 × 500 像素或更大；
- 建议使用 PNG 或 JPG；
- 文件名尽量只使用英文、数字和连字符。

### 2. 放入图片文件夹

把头像复制到：

```text
hexo-blog\source\img\avatar.png
```

当前项目已经有一个默认头像：

```text
source\img\avatar.svg
```

可以直接用新图片替换，也可以保留默认文件并使用新的文件名。

### 3. 修改头像配置

打开 `_config.fluid.yml`，找到：

```yaml
about:
  enable: true
  banner_img: /img/banner.svg
  banner_img_height: 60
  banner_mask_alpha: 0.3
  avatar: /img/avatar.svg
  name: "你的名字"
  intro: "技术、设计与生活的长期记录者"
```

如果新头像叫 `avatar.png`，改成：

```yaml
  avatar: /img/avatar.png
```

注意：

- 网站路径写 `/img/avatar.png`；
- 实际文件放在 `source\img\avatar.png`；
- YAML 缩进必须保留，建议使用两个空格；
- 路径中的 `/` 不要改成 Windows 的 `\`。

---

## 四、修改个人简介和社交链接

打开 `_config.fluid.yml`，找到 `about`：

```yaml
about:
  enable: true
  banner_img: /img/banner.svg
  banner_img_height: 60
  banner_mask_alpha: 0.3
  avatar: /img/avatar.png
  name: "你的名字"
  intro: "技术、设计与生活的长期记录者"
  icons:
    - { class: "iconfont icon-github-fill", link: "https://github.com/你的用户名", tip: "GitHub" }
    - { class: "iconfont icon-mail", link: "mailto:hello@example.com", tip: "Email" }
```

示例：

```yaml
about:
  enable: true
  banner_img: /img/banner.svg
  banner_img_height: 60
  banner_mask_alpha: 0.3
  avatar: /img/avatar.png
  name: "张三"
  intro: "记录学习、技术实践与日常生活"
  icons:
    - { class: "iconfont icon-github-fill", link: "https://github.com/zhangsan", tip: "GitHub" }
    - { class: "iconfont icon-mail", link: "mailto:zhangsan@example.com", tip: "Email" }
```

替换内容：

- `张三`：你的昵称或姓名；
- `zhangsan`：你的 GitHub 用户名；
- `zhangsan@example.com`：希望公开显示的邮箱。

如果不希望公开邮箱，可以删除这一整行：

```yaml
- { class: "iconfont icon-mail", link: "mailto:zhangsan@example.com", tip: "Email" }
```

不要把 GitHub 密码、验证码、访问令牌或其他密钥写进配置文件。

---

## 五、修改关于页面

关于页面文件位于：

```text
source\about\index.md
```

可以修改成：

```markdown
---
title: 关于我
layout: about
---

欢迎来到 **时光笔记**。

我是张三，这里主要记录我的学习过程、技术实践和生活感悟。

## 我在做什么

- 学习编程和软件开发
- 整理每周学习内容
- 记录值得保存的生活片段

## 联系我

- GitHub：填写你的 GitHub 主页
- Email：填写你愿意公开的邮箱
```

文件开头的 `---`、`title` 和 `layout` 不要删除。

---

## 六、修改首页标语、横幅和页脚

这些内容都在 `_config.fluid.yml` 中。

### 1. 修改顶部博客名称

```yaml
navbar:
  blog_title: "拾光笔记"
```

### 2. 修改首页轮换标语

```yaml
index:
  slogan:
    enable: true
    text:
      - "认真记录，每一次发现"
      - "保持好奇，持续创造"
      - "把复杂的世界，写成清楚的文字"
```

可以改成：

```yaml
index:
  slogan:
    enable: true
    text:
      - "记录每周的成长"
      - "保持学习，保持思考"
      - "把走过的路写下来"
```

### 3. 修改首页背景图

把新背景图放到：

```text
source\img\banner.jpg
```

然后修改：

```yaml
index:
  banner_img: /img/banner.jpg
```

文章页、归档页、分类页、标签页和关于页也分别有 `banner_img`，可以全部使用同一张图片。

### 4. 修改页脚文字

```yaml
footer:
  content: '愿每一次记录，都成为时间里的微光。'
```

例如：

```yaml
footer:
  content: '持续学习，认真生活。'
```

### 5. 修改网站图标

将图标文件放入 `source\img`，例如：

```text
source\img\favicon.png
```

修改 `_config.fluid.yml`：

```yaml
favicon: /img/favicon.png
apple_touch_icon: /img/favicon.png
```

---

## 七、新建和编辑文章

### 1. 新建文章

最简单的方法是双击：

```text
新建文章.cmd
```

也可以打开 PowerShell，执行：

```powershell
cd "C:\Users\xwh\Documents\Codex\2026-09-19\ji\outputs\hexo-blog"
npx hexo new "文章标题"
```

如果 PowerShell 提示禁止运行脚本，使用：

```powershell
npx.cmd hexo new "文章标题"
```

新文章保存在：

```text
source\_posts
```

例如：

```text
source\_posts\2026-9-14-2026-9-20.md
```

### 2. 文章基本格式

```markdown
---
title: 2026.9.14-2026.9.20
date: 2026-09-19 16:22:17
categories:
  - 周记
tags:
  - 学习
  - 生活
---

## 本周完成

1. 完成第一项任务
2. 学习新的知识
3. 整理本周笔记

## 遇到的问题

这里填写遇到的问题和解决办法。

## 下周计划

- 完成下一项任务
- 继续学习
```

文章正文写在第二个 `---` 的下面。

### 3. 常用 Markdown 语法

```markdown
# 一级标题
## 二级标题
### 三级标题

**粗体文字**
*斜体文字*

- 无序列表
- 第二项

1. 有序列表
2. 第二项

[链接文字](https://example.com)

`一小段代码`

> 引用内容
```

代码块：

````markdown
```python
print("Hello, World!")
```
````

编辑完成后按 `Ctrl + S` 保存。

---

## 八、在文章中插入图片

### 方法一：使用站点公共图片

把图片放到：

```text
source\img\photo.jpg
```

文章中写：

```markdown
![图片说明](/img/photo.jpg)
```

这种方式适合头像、网站背景和多篇文章共同使用的图片。

### 方法二：使用文章专属图片文件夹

项目已启用：

```yaml
post_asset_folder: true
```

创建文章时，Hexo 会同时创建同名文件夹。例如：

```text
source\_posts\2026-9-14-2026-9-20.md
source\_posts\2026-9-14-2026-9-20\
```

将图片放入同名文件夹：

```text
source\_posts\2026-9-14-2026-9-20\photo.jpg
```

文章中可以写：

```markdown
{% asset_img photo.jpg 本周照片 %}
```

图片文件名建议使用英文或数字，避免空格和特殊符号。

---

## 九、本地预览和生成网站

### 1. 本地预览

双击：

```text
启动博客.cmd
```

或者运行：

```powershell
cd "C:\Users\xwh\Documents\Codex\2026-09-19\ji\outputs\hexo-blog"
npm run server
```

如果遇到 PowerShell 脚本策略错误：

```powershell
npm.cmd run server
```

浏览器打开：

```text
http://localhost:4000
```

保存文章后，在浏览器中按 `F5` 刷新查看。

停止服务器时，在终端中按：

```text
Ctrl + C
```

### 2. 生成静态网站

双击：

```text
生成网站.cmd
```

或者运行：

```powershell
npm run build
```

生成结果位于：

```text
public
```

`public` 是自动生成的文件夹，不需要手工编辑。

---

## 十、发布到 GitHub Pages

### 1. 创建 GitHub 仓库

假设 GitHub 用户名是 `zhangsan`，推荐仓库名：

```text
zhangsan.github.io
```

这样网站地址是：

```text
https://zhangsan.github.io/
```

创建仓库时：

- 选择 `Public`；
- 不要勾选自动创建 README；
- 不要添加新的 `.gitignore`；
- 不要添加 License。

### 2. 设置 Git 信息

在 PowerShell 中进入博客目录：

```powershell
cd "C:\Users\xwh\Documents\Codex\2026-09-19\ji\outputs\hexo-blog"
```

设置用户名和邮箱：

```powershell
git config --global user.name "你的GitHub用户名"
git config --global user.email "你的GitHub邮箱"
```

如果出现 `detected dubious ownership`：

```powershell
git config --global --add safe.directory "C:/Users/xwh/Documents/Codex/2026-09-19/ji/outputs/hexo-blog"
```

### 3. 第一次提交

```powershell
git add .
git commit -m "创建拾光笔记"
git branch -M main
```

### 4. 连接远程仓库

将 `你的用户名` 替换为真实 GitHub 用户名：

```powershell
git remote add origin https://github.com/你的用户名/你的用户名.github.io.git
```

检查：

```powershell
git remote -v
```

如果提示 `remote origin already exists`，使用：

```powershell
git remote set-url origin https://github.com/你的用户名/你的用户名.github.io.git
```

### 5. 上传

```powershell
git push -u origin main
```

第一次推送可能会打开浏览器，请在浏览器中登录并授权。不要把密码、验证码或访问令牌发送给其他人。

### 6. 开启 GitHub Pages

进入 GitHub 仓库：

1. 点击 `Settings`；
2. 点击左侧 `Pages`；
3. 在 `Build and deployment` 中找到 `Source`；
4. 选择 `GitHub Actions`；
5. 打开仓库顶部的 `Actions`；
6. 等待 `Deploy Hexo to GitHub Pages` 出现绿色对勾。

项目已经包含自动发布文件：

```text
.github\workflows\pages.yml
```

发布成功后访问：

```text
https://你的用户名.github.io/
```

如果使用普通仓库名，例如 `shiguang-blog`，地址通常是：

```text
https://你的用户名.github.io/shiguang-blog/
```

---

## 十一、以后如何更新网站

每次修改文章或配置后，在博客目录中执行：

```powershell
git add .
git commit -m "更新博客"
git push
```

例如发布周记：

```powershell
git add .
git commit -m "发布周记 2026.9.14-2026.9.20"
git push
```

GitHub Actions 会自动重新生成并发布网站。

建议的完整工作流程：

1. 新建或编辑文章；
2. `Ctrl + S` 保存；
3. 使用 `npm run server` 本地预览；
4. 检查页面、图片和链接；
5. 按 `Ctrl + C` 停止服务器；
6. 执行 `git add .`；
7. 执行 `git commit -m "说明"`；
8. 执行 `git push`；
9. 在 GitHub 的 `Actions` 页面确认部署成功。

---

## 十二、常见问题

### 1. `node`、`npm` 或 `npx` 无法识别

关闭所有 PowerShell 窗口并重新打开，然后检查：

```powershell
node --version
npm --version
npx --version
```

当前安装版本：

```text
Node.js v24.21.0
npm 11.19.0
npx 11.19.0
```

### 2. PowerShell 提示“禁止运行脚本”

使用 `.cmd` 版本：

```powershell
npm.cmd run server
npx.cmd hexo new "文章标题"
```

项目中的快捷脚本已经使用 `.cmd`，不需要修改 PowerShell 安全策略。

### 3. 命令提示找不到 Hexo

确认当前目录是博客目录：

```powershell
cd "C:\Users\xwh\Documents\Codex\2026-09-19\ji\outputs\hexo-blog"
```

然后使用：

```powershell
npx.cmd hexo --version
```

### 4. 修改后页面没有变化

依次尝试：

```powershell
npm run build
npm run server
```

然后在浏览器中按 `Ctrl + F5` 强制刷新。

### 5. YAML 配置错误

YAML 对缩进敏感。注意：

- 使用空格，不要使用 Tab；
- 同一级配置保持相同缩进；
- 包含冒号的复杂文字建议加引号；
- 修改前可以复制一份文件作为备份。

### 6. GitHub Actions 发布失败

进入 GitHub 仓库的 `Actions` 页面：

1. 点击失败的工作流；
2. 打开带红色叉号的步骤；
3. 查看最后几行错误；
4. 把错误内容发给 Codex 协助处理。

### 7. 图片在 GitHub Pages 中不显示

检查：

- 图片是否确实上传到了 GitHub；
- 文件名大小写是否完全一致；
- 路径是否以 `/img/` 开头；
- 不要在网页路径中使用 `C:\Users\...` 本地路径；
- 尽量避免中文文件名和空格。

---

## 十三、文件结构速查

```text
hexo-blog
├─ .github
│  └─ workflows
│     └─ pages.yml              # GitHub Pages 自动发布
├─ scaffolds                    # 新文章模板
├─ source
│  ├─ _posts                    # 所有文章
│  ├─ about
│  │  └─ index.md               # 关于页面正文
│  └─ img                       # 头像、背景和公共图片
├─ themes
│  └─ fluid                     # Fluid 主题程序
├─ _config.yml                  # Hexo 网站基本配置
├─ _config.fluid.yml            # Fluid 外观和个人信息配置
├─ package.json                 # 依赖和常用命令
├─ 启动博客.cmd                 # 本地预览
├─ 新建文章.cmd                 # 新建文章
├─ 生成网站.cmd                 # 生成静态网页
└─ 拾光笔记-完整操作指南.md       # 本指南
```

## 常用命令速查

```powershell
# 进入博客目录
cd "C:\Users\xwh\Documents\Codex\2026-09-19\ji\outputs\hexo-blog"

# 新建文章
npx.cmd hexo new "文章标题"

# 本地预览
npm.cmd run server

# 生成网站
npm.cmd run build

# 第一次提交或提交新修改
git add .
git commit -m "更新说明"
git push
```

---

完成个性化时，建议按以下顺序修改：

1. `_config.yml` 中的站点名称和作者；
2. `_config.fluid.yml` 中的导航标题、头像、姓名、简介、GitHub 和邮箱；
3. `source\about\index.md` 中的详细自我介绍；
4. `source\img` 中的头像、横幅和网站图标；
5. 启动本地博客检查效果；
6. 确认无误后提交并推送到 GitHub。